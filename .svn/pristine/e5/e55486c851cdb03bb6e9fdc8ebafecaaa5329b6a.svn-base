#
# description:
# author:wuhongliang
# date:2015-11-05 14:00:18
# modify:
#
    testcase{
      attr = {"id"=>"ZLBF_6.1.28", "level"=>"P1", "auto"=>"n"}
    def prepare

    end

    def process

      operate("1、BAS设置dns中添加两个DNS地址，DNS1为有效的DNS地址，DNS2为无效的DNS地址；") {

}

operate("2、DUT上配置相应的PPPoE 方式接入配置，DNS选择自动从ISP获取，保存；") {

}

operate("3、LAN PC上在DOS下输入:ipconfig/flushdns，清除PC的DNS缓存,执行ping www.sohu.com，是否可以解释成功；") {

}

operate("4、BAS设置dns中添加两个DNS地址，DNS1为无效的DNS地址，DNS2为有效的DNS地址；") {

}

operate("5、重复步骤3、4。") {

}

operate("备注：在DUT中采用cat /etc/resolve.conf与运行状态DNS信息是否一致。") {

}



    end

    def clearup

    end

    }
